# Third-party components

Two libraries are bundled unmodified so the page works offline. Both are
distributed under the MIT License by their respective authors:

* `plotly.min.js` — Plotly.js, https://plotly.com/javascript/
* `three.min.js`, `TrackballControls.js` — three.js, https://threejs.org

The remaining files (`index.html`, `kinematics.js`, `materials_manifest.js`
and the `navigator_data_*.js` payloads) belong to this project and are
covered by the MIT license in `LICENSE`.

Sources and citations for the phonon datasets are given in `README.md`.
